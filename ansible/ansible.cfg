[defaults]
host_key_checking = False
remote_user = ec2-user
roles_path = roles
inventory = inventories/aws/hosts
private_key_file = ~/.ssh/terraform.pem

[ssh_connection]
ssh_args = -o ForwardAgent=yes -o ControlMaster=auto -o ControlPersist=30m -o ServerAliveInterval=60
